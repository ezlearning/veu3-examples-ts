/* eslint-disable */
// @ts-check
const path = require("path");
const fs = require("fs");

const cwd = process.cwd();
const dir = __dirname;
const pwd = process.env.PWD;

console.log("pwd", process.env.INIT_CWD);
