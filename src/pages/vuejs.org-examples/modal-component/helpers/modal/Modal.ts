import {
  defineComponent,
  ref,
  reactive,
  PropType,
  computed,
  onMounted,
} from "vue";

export default defineComponent({
  name: "Modal",

  components: {},

  props: {},

  setup() {
    onMounted(() => {});

    return {};
  },
});
